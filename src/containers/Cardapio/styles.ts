import styled from "styled-components";
import { TabPanel } from "react-tabs";

export const TabPanelFoods = styled(TabPanel)`
  display: grid;
  gap: 22px;
  grid-template-columns: repeat(3, 1fr);
`;


